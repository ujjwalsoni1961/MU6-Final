// Stub: @coinbase/wallet-mobile-sdk is not installed.
// Thirdweb dynamically imports this for Coinbase Wallet support.
// Since we don't use Coinbase Wallet, this stub prevents the crash.
module.exports = {
    configure: () => {},
    handleResponse: () => {},
};
